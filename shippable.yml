language: node_js

#node_js:
  #- 4.3.1
  #- 5.0.0


build:
  cache: true
  pre_ci:
    - echo "this is pr-ci"
  ci:
    - echo "this is ci pulling private image from docker"
    #- docker pull riturajcse/rituexm
    - printenv
    
      
