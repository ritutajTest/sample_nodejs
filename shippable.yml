language: node_js

node_js:
  - 4.3.1
  - 5.0.0

env:
  global:
    - XUNIT_FILE=shippable/testresults/result.xml
    - foo=bar
    - foo1=bar1

build:
  cache: true
  pre_ci:
    - echo "this is pr-ci"
  ci:
    - npm install
    - npm test
      
